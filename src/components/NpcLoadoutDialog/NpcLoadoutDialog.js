import React from 'react';

import Dialog from '@mui/material/Dialog';

const NpcLoadoutDialog = ({ open }) => {
    return (
        <Dialog
            open={open}
        >
            Hi
        </Dialog>
    );
};

export default NpcLoadoutDialog;